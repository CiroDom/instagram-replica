<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingHorizontal="36dp"
    android:background="@color/white"
    tools:context=".ui.login.LoginActivity">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.45"
        android:orientation="vertical">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="150dp"
            android:layout_marginBottom="150dp"
            android:src="@drawable/photo_icon"/>

        <TextView android:id="@+id/sign_up_txt_photo_title"
            style="@style/Theme.Instagram.TextViewBase.Form"
            android:text="@string/photo_register_title"
            android:layout_gravity="center"
            android:textSize="24sp"
            android:textColor="@color/black"/>

        <TextView android:id="@+id/sign_up_txt_photo_subtitle"
            style="@style/Theme.Instagram.TextViewBase.Form"
            android:text="@string/photo_register_subtitle"
            android:layout_gravity="center"
            android:textColor="@color/black"/>

        <co.tiagoaguiar.course.instagram.ui.login.LoadingButton
            android:id="@+id/sign_up_button_next_photo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:text="@string/next"/>
        
        <Button android:id="@+id/sign_up_button_photojump"
            style="@style/Theme.Instagram.Link"
            android:text="@string/jump"/>

    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>